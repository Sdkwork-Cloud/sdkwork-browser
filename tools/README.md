# SDKWork Browser Tools

Reserved for repository-local developer and operator tools that are not shipped as runtime code.

Canonical database and API validation tools live in `../sdkwork-specs/tools/` and are invoked through root `pnpm` scripts.
