# Mobile application surfaces

| SDKWork root | Platform |
| --- | --- |
| [`../sdkwork-browser-h5`](../sdkwork-browser-h5) | H5 / Capacitor React |
| [`../sdkwork-browser-flutter-mobile`](../sdkwork-browser-flutter-mobile) | Flutter iOS/Android |

Both consume shared contracts from `packages/common/browser/` and the same Browser Engine Standard IDs (`webview`, `servo`, `cef`).
